# Production build config for the GitHub Pages deployment
# (served at https://aguzmancruz.com/local-rag).
# The GitHub Action can override this via the VITE_API_BASE_URL build env.
VITE_API_BASE_URL=https://api.aguzmancruz.com
