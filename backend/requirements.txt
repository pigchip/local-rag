# Local RAG backend dependencies (FastAPI + local retrieval + provider APIs)

# --- Web API ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
httpx>=0.27.0

# --- Headless RAG: retrieval only, no text generation ---
haystack-ai>=2.0.0
lancedb-haystack>=0.1.1
# lancedb 0.13.0 is the last release whose write path is stable with
# lancedb-haystack 0.1.1 on Windows (newer releases panic on merge-insert).
lancedb==0.13.0

# --- Local embeddings (no API key required) ---
sentence-transformers>=2.2.0
# Explicit: used for optional data persistence to a HF Dataset (deploy/hf-space).
huggingface_hub>=0.23.0
# Pinned to stay compatible with lancedb 0.13.0 (which requires numpy<2).
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.2

# --- Document parsing ---
pypdf>=4.0.0
markdown-it-py>=3.0.0
mdit-plain>=1.0.0

# --- Config / env ---
pydantic>=2.0.0
pydantic-settings>=2.0.0

# --- Optional: local HuggingFace generation (LLM_PROVIDER=local) ---
# Uncomment to run answer generation fully offline on CPU.
# transformers>=4.44.0
# accelerate>=0.30.0
# torch>=2.2.0
